package hbcu.stay.ready.scientificcalculator;
import java.util.Collections;
public class ScientificFeatures {

    Display display = new Display();

    double displayMemory = 0;

    public void addMemory (double x)(display.setCurrentNumber(x); displayMemory = x;
    )

    public void recallMemory {display.getCurrentNumber(); }

    public void sine () {display.setCurrentNumber{Math.sin(display.getCurrentNumber()));}

    public void cosine () {display.setCurrentNumber{Math.sin(display.getCurrentNumber()));}
    public void tangent (){display.setCurrentNumber(Math.tan(display.getCurrentNumber()));)


}
